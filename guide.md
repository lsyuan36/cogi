這是一份整合了**視覺設計系統（柯基配色）**、**Vue.js 技術架構**、**詳細頁面規劃**以及**Demo 運營腳本**的完整開發規格書。這份文件可以直接作為開發團隊（或您個人）的實作藍圖。

-----

# CorgiRisk 柯基風控｜前端 Demo 開發規格書

## 1\. 設計規範：柯基配色系統 (Design System)

基於您提供的色卡，我們建立了一套「溫暖但專業」的 Fintech 配色系統。這套系統將柯基的毛色轉化為介面元素，既親切又能透過深色背景維持科技感。

### 1.1 色彩角色定義

| 色彩角色 | 建議色號 | 變數名稱 (Tailwind) | 應用場景 |
| :--- | :--- | :--- | :--- |
| **Primary (主色)** | **\#E69138** (亮橘色) | `corgi-primary` | 主要 CTA 按鈕、Loading 動畫、高亮數據 |
| **Secondary (輔色)** | **\#D2691E** (橘棕色) | `corgi-secondary` | 次要按鈕、Hover 狀態、圖表線條 |
| **Surface (淺色介面)** | **\#F5DEB3** (米黃色) | `corgi-surface` | 卡片背景、淺色模式底色、面板區塊 |
| **Dark BG (深色背景)**| **\#1C1C1C** (極深灰黑) | `corgi-dark` | App 整體背景、深色模式、導覽列 |
| **Accent (點綴)** | **\#F4A460** (淺橘色) | `corgi-accent` | 雷達圖填充色、Badge 標籤背景 |
| **Text (深色文字)** | **\#2B2B2B** (深灰黑色) | `corgi-text-dark` | 標題、正文（淺色卡片上） |
| **Text (淺色文字)** | **\#D2B48C** (淡棕色) | `corgi-text-light` | 副標題、深色背景上的文字 |
| **Warning (風險/警示)** | **\#8B4513** (馬鞍棕) | `corgi-warning` | 風險紅旗 (Red Flag)、錯誤訊息、低分項目 |

### 1.2 Tailwind 配置 (`tailwind.config.js`)

請將以下設定複製到您的專案設定檔中：

```javascript
export default {
  theme: {
    extend: {
      colors: {
        corgi: {
          dark: '#1C1C1C',       // 極深灰黑
          darker: '#2B2B2B',     // 深灰黑色 (用於深色卡片背景)
          primary: '#E69138',    // 亮橘色
          secondary: '#D2691E',  // 橘棕色
          surface: '#F5DEB3',    // 米黃色
          accent: '#F4A460',     // 淺橘色
          warning: '#8B4513',    // 馬鞍棕
          text: {
            DEFAULT: '#2B2B2B',  // 深灰黑色
            light: '#D2B48C',    // 淡棕色
            muted: '#5C4A3A',    // 深棕色
          }
        }
      },
      fontFamily: {
        sans: ['Inter', 'Noto Sans TC', 'sans-serif'],
      }
    }
  }
}
```

-----

## 2\. 技術架構 (Technical Architecture)

由於是 Demo 性質，採用 **"Frontend Only"** 策略，利用前端狀態管理模擬後端邏輯。

  * **核心框架：** Vue 3 (Composition API) + Vite
  * **路由管理：** Vue Router (控制 5 大頁面切換)
  * **狀態管理：** **Pinia** (關鍵核心：負責在頁面間傳遞「假數據」，讓系統看起來是連動的)
  * **圖表視覺：** Chart.js 或 ECharts (繪製雷達圖與 LSTM 曲線)
  * **樣式庫：** Tailwind CSS
  * **模擬策略：**
      * 無後端 API，所有 `fetch` 動作皆使用 `setTimeout` 模擬網路延遲。
      * 照片上傳功能直接讀取 `src/assets/images` 內的預設圖片。

### 2.1 專案目錄結構

```text
src/
├── assets/
│   ├── images/          # 預存的假照片 (機房亂照、ISO證書、柯基插圖)
│   └── css/             # tailwind.css
├── components/
│   ├── CorgiRadar.vue   # 封裝好的雷達圖組件
│   ├── LoadingBone.vue  # 柯基骨頭形狀的 Loading 動畫
│   └── MobileFrame.vue  # 給行員端用的手機外框 (限制寬度 375px)
├── stores/
│   └── riskStore.js     # Pinia: 存放 "演示用企業" 的所有評分與狀態
├── views/
│   ├── Landing.vue      # 首頁
│   ├── Business.vue     # 企業專區 (科技體檢)
│   ├── Staff.vue        # 行員專區 (手機版訪查)
│   ├── Credit.vue       # 授信專區 (動態評分)
│   └── Admin.vue        # 管理後台
├── App.vue
└── main.js
```

-----

## 3\. 詳細頁面 UI 規劃與實作重點

### 3.1 首頁 (Landing Page)

  * [cite\_start]**目標：** 建立信任、展示價值、分流 [cite: 1-4]。
  * **UI 配置：**
      * **背景：** `bg-corgi-dark` 搭配動態粒子連線效果。
      * **Hero 標題：** 「讓科技風險成為可見、可查、可備查的信用資產」。
      * **分流卡片：** 三張並排的 `bg-corgi-surface` 毛玻璃卡片。
          * 🏢 **我是企業：** 連結至 `/business`
          * 💼 **我是行員：** 連結至 `/staff`
          * 🔍 **我是授信審核：** 連結至 `/credit-review`
  * **Demo 策略：** 點擊卡片直接轉跳，不製作登入/註冊流程。

### 3.2 企業專區 (Business View)

  * [cite\_start]**功能：** 科技體檢模組 (Level 1\~3) [cite: 153, 160]。
  * **UI 配置：**
      * **佈局：** Bento Grid (便當盒式)，背景 `bg-corgi-surface`。
      * [cite\_start]**Dashboard：** 中央顯示大型風險評級 (例如：B級)，右側為六大構面雷達圖 [cite: 194-203]。
          * **六大構面：** 系統穩定性 (Stability)、資安成熟度 (Security)、科技治理 (Governance)、供應商依賴度 (Supplier)、AI 使用風險 (AI Risk)、趨勢適應力 (Trend)。
      * **引導式填寫 (Wizard)：**
          * [cite\_start]**Level 1 自證：** Checkbox 勾選「是否有 IT 人員」、「備份頻率」[cite: 170-171]。
          * **Level 2 串接：** 按鈕「連結 API」，點擊後顯示 Loading 動畫 (模擬讀取 Uptime、錯誤日誌)。
          * [cite\_start]**Level 3 認證：** 拖拉上傳區 (Dropzone)，支援 ISO 27001 / 弱掃報告 [cite: 173]。
  * **Demo 策略：**
      * 設定一個「一鍵填寫」按鈕（隱藏或顯眼皆可），點擊後自動跑完 API 串接動畫，並將 Pinia 的 `riskScore` 設為初始值。

### 3.3 行員專區 (Staff View) - Mobile First

  * [cite\_start]**功能：** 訪查輔助 (檢索 -\> 判斷 -\> 留證) [cite: 250-251]。
  * **UI 配置：**
      * **外框：** 模擬手機螢幕寬度 (max-w-sm)，置中顯示，背景 `bg-corgi-dark` (減少反光)。
      * [cite\_start]**Tab 1 檢索：** 顯示企業「科技輪廓摘要」（例：高度依賴單一 POS 廠商）[cite: 262]。
      * [cite\_start]**Tab 2 判斷：** 動態檢核表。當勾選與企業自填不符的項目時，彈出 `text-corgi-warning` 的警示 [cite: 266]。
      * **Tab 3 留證：** 底部大型橘色按鈕 (`bg-corgi-primary`)。
          * 📷 **拍照：** 模擬相機介面。
          * 🎤 **錄音：** 語音轉文字介面。
  * **Demo 策略：**
      * **相機模擬：** 點擊拍照按鈕 -\> 顯示 1 秒 Loading -\> **直接顯示一張預存的「機房亂七八糟」照片**。
      * **Pinia 連動：** 拍照確認後，將 `hasEvidence` 設為 `true`，並將該照片 URL 存入 Store，供後台調用。

### 3.4 授信審核專區 (Credit Review View)

  * [cite\_start]**功能：** 產業權重調整、動態授信模型 [cite: 209, 316]。
  * **UI 配置：**
      * **左欄：** 企業基本資料 + 風險評級。
      * **中欄 (模擬器)：**
          * [cite\_start]**產業下拉選單：** 選擇不同產業，權重自動調整：
              * **零售業：** 系統穩定性 +20% (POS 依賴)、供應商依賴 +15%。
              * **製造業：** 供應商依賴 +30% (產線停擺風險)、科技治理 +10%。
              * **醫療業：** 資安成熟度 +35% (個資敏感)、AI 風險 +20%。
          * [cite\_start]**LSTM 預測圖：** 顯示違約率曲線。提供一個 Toggle 開關「導入 CorgiRisk」，開啟後曲線精準度提升 [cite: 316]。
      * **右欄 (證據追溯)：**
          * 當滑鼠 Hover 到「資安分數」低分項目時，自動滑出佐證面板。
  * **Demo 策略 (亮點)：**
      * **跨頁連動：** 當使用者在「右欄」查看時，顯示的正是剛剛在「行員端」拍的那張機房照片。這能強力證明「資料流」是通的。

### 3.5 管理後台 (Admin View)

  * [cite\_start]**功能：** 全局監控、爬蟲狀態 [cite: 304, 311]。
  * **UI 配置：** 深色儀表板風格。
      * [cite\_start]**爬蟲狀態：** 顯示 `Selenium` 正在抓取法規，跳出 Toast 通知「已更新歐盟 AI 法案」[cite: 48]。
      * **技術監控：** 顯示 FastAPI 延遲與 Gemini Token 用量。
      * **模型效益：** 顯示 LSTM 動態授信模型準確率提升 **37%** (vs 傳統靜態模型)。

-----

## 4\. Pinia 狀態管理邏輯 (`riskStore.js`)

這是 Demo 能夠「動起來」的核心。

```javascript
// stores/riskStore.js
import { defineStore } from 'pinia'

export const useRiskStore = defineStore('risk', {
  state: () => ({
    companyName: '汪汪餐飲集團',
    industry: 'retail', // retail, medical, manufacturing
    baseScore: 80,
    // 六大構面細項分數 (初始值)
    dimensions: {
      stability: 75,  // 系統穩定性
      security: 70,   // 資安成熟度
      governance: 60, // 科技治理
      supplier: 85,   // 供應商依賴度
      aiRisk: 90,     // AI 使用風險 (分數越高風險越低)
      trend: 65       // 趨勢適應力
    },
    evidencePhoto: null, // 存放行員拍的照片
    redFlags: [], // 存放被觸發的紅旗警示
    isApiConnected: false, // 是否已串接 Level 2 數據
  }),
  getters: {
    finalScore: (state) => {
      // 簡單的模擬算法：如果有紅旗，分數大扣
      let deduction = state.redFlags.length * 15;
      return state.baseScore - deduction;
    },
    riskLevel: (state) => {
      const score = state.finalScore;
      if (score >= 80) return 'A';
      if (score >= 70) return 'B';
      return 'C';
    }
  },
  actions: {
    addEvidence(photoUrl) {
      this.evidencePhoto = photoUrl;
      this.redFlags.push('現場資安環境與描述不符');
    },
    connectApi() {
      this.isApiConnected = true;
      // 模擬 API 發現問題，分數微調
      this.baseScore += 5; 
    }
  }
})
```
